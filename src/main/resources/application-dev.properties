# Nur fuer lokale Entwicklung: ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
# Dieses Secret steht oeffentlich im Repo - jeder kann damit Tokens faelschen. Genau deshalb
# lebt es nur in diesem Profil und nicht in application.properties.
promptlib.jwt.secret=dev-only-secret-key-never-use-outside-local-development-32bytes+
